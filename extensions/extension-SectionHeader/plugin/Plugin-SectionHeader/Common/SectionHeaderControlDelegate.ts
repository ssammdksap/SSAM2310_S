export class SectionHeaderControlDelegate {

    public static initWithExtension(controlExtension): SectionHeaderControlDelegate {
        return new SectionHeaderControlDelegate();
    }

    public onCreate() {
        // intentional no-op
    }

    public onPress(action) {
        // intentional no-op
    }

    public localizedValue(key, params): any {
        // intentional no-op
    }
}
